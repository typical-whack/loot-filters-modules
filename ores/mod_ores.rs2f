// module:ores

#define CONST_DEFAULT_MENU_TEXT_COLOR "#FFFF9040"

#define VAR_ORES_HIDE []

#define VAR_COPPER_ORE_CUSTOMSTYLE nop;
#define VAR_TIN_ORE_CUSTOMSTYLE nop;
#define VAR_BLURITE_ORE_CUSTOMSTYLE nop;
#define VAR_IRON_ORE_CUSTOMSTYLE nop;
#define VAR_DAEYALT_ORE_CUSTOMSTYLE nop;
#define VAR_SILVER_ORE_CUSTOMSTYLE nop;
#define VAR_COAL_ORE_CUSTOMSTYLE nop;
#define VAR_GOLD_ORE_CUSTOMSTYLE nop;
#define VAR_BLASTED_ORE_CUSTOMSTYLE nop;
#define VAR_MITHRIL_ORE_CUSTOMSTYLE nop;
#define VAR_LUNAR_ORE_CUSTOMSTYLE nop;
#define VAR_LOVAKITE_ORE_CUSTOMSTYLE nop;
#define VAR_ADAMANTITE_ORE_CUSTOMSTYLE nop;
#define VAR_RUNITE_ORE_CUSTOMSTYLE nop;

#define VAR_ORES_COLOR_MENU_TEXT nop;

if (name:"copper ore") {
    if ("copper ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_COPPER_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"tin ore") {
    if ("tin ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_TIN_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"blurite ore") {
    if ("blurite ore":VAR_ORES_HIDE) {
        hidden = true;
    })
    VAR_BLURITE_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"iron ore") {
    if ("iron ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_IRON_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"daeyalt ore") {
    if ("daeyalt ore":VAR_ORES_HIDE) {
        hidden = true;
    })
    VAR_DAEYALT_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"silver ore") {
    if ("silver ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_SILVER_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"coal ore") {
    if ("coal ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_COAL_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"gold ore") {
    if ("gold ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_GOLD_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"blasted ore") {
    if ("blasted ore":VAR_ORES_HIDE) {
        hidden = true;
    })
    VAR_BLASTED_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"mithril ore") {
    if ("mithril ore":VAR_ORES_HIDE) {
        hidden = true;
    })
    VAR_MITHRIL_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"lunar ore") {
    if ("lunar ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_LUNAR_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"lovakite ore") {
    if ("lovakite ore":VAR_ORES_HIDE) {
        hidden = true;
    })
    VAR_LOVAKITE_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"adamantite ore") {
    if ("adamantite ore":VAR_ORES_HIDE) {
        hidden = true;
    })
    VAR_ADAMANTITE_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

if (name:"runite ore") {
    if ("runite ore":VAR_ORES_HIDE) {
        hidden = true;
    }
    VAR_RUNITE_ORE_CUSTOMSTYLE
    if (!VAR_ORES_COLOR_MENU_TEXT) {
        menuTextColor=CONST_DEFAULT_MENU_TEXT_COLOR
    }
}

// endmodule:ores
