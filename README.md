# loot-filters-modules

For use with loot filters runelite plugin and the filterscape UI
https://filterscape.xyz/

# TODO
- split up uniques list into a constant that is a union of a constant unique list for each boss, makes it easier to manage the list overall?
- move basically everything out of constants and into user viewable website lists so they can add items if they need to
- add boolean toggles for groups of styling (since the website is missing this)
- cut a version of the filter that is "blank slate" style-wise with it JUST matching the value tiers from ground items
- maybe have one that is like property based? where theres a section early on for JUST icons? - idk this is confusing since it has a whole style pane and im selecting by category
